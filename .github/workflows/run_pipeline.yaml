name: run_pipeline
on: [push]
jobs:
  run:
    runs-on: [self-hosted,cml,gpu]
    steps:
      - uses: actions/checkout@v2
      - name: cml_run
        run: |
          echo "--- RUN ---" > report.md
          nvidia-smi >> report.md
          cml send-comment report.md
